<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!--
      Declared here rather than in app.json, because a service is not a
      permission and app.json cannot express one. Expo merges this module's
      manifest into the app's at build time.
    -->
    <service
      android:name="expo.modules.reelcounter.ReelCounterService"
      android:exported="false"
      android:label="@string/reel_counter_service_label"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/reel_counter_service" />
    </service>
  </application>
</manifest>
